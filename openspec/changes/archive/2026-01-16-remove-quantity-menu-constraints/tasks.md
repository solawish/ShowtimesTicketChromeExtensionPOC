## 1. 實作
- [x] 1.1 在 `popup.html` 中，將數量下拉選單的 `disabled` 屬性移除
- [x] 1.2 在 `popup.js` 的初始化流程中，在 `DOMContentLoaded` 時呼叫 `updateQuantityOptions()` 來初始化數量選單
- [x] 1.3 修改 `resetDependentMenus()` 函數，移除對數量選單的重置邏輯（不再將數量選單設為禁用）
- [x] 1.4 移除所有在票種選擇變更時對數量選單的禁用操作
- [x] 1.5 確保數量選單在 popup 開啟時就顯示選項 1-6 並為啟用狀態

## 2. 驗證
- [x] 2.1 驗證數量選單在 popup 開啟時即為啟用狀態
- [x] 2.2 驗證數量選單在未選擇任何前置選單時仍可選擇
- [x] 2.3 驗證變更電影、場地、時間、票種時，數量選單保持啟用狀態
- [x] 2.4 驗證數量選單的選項始終為 1-6
- [x] 2.5 驗證選擇數量後，狀態正確儲存到 Chrome Storage
