## 1. 實作
- [x] 1.1 在 `popup.html` 中，於票種下拉選單的 form-group 內新增「重新讀取」按鈕元素
- [x] 1.2 在 `popup.css` 中新增按鈕樣式，使其與現有 UI 風格一致
- [x] 1.3 在 `popup.js` 的 `elements` 物件中新增按鈕元素的引用
- [x] 1.4 在 `setupEventListeners()` 中為按鈕新增點擊事件監聽器
- [x] 1.5 實作按鈕點擊處理函數，呼叫 `loadTicketTypes()` 重新載入票種資料
- [x] 1.6 確保按鈕在票種選單禁用時也禁用（即未選擇時間時）
- [x] 1.7 確保重新載入時保留當前選定的票種（如果仍存在）

## 2. 驗證
- [x] 2.1 驗證按鈕在票種選單啟用時可點擊
- [x] 2.2 驗證按鈕在票種選單禁用時也禁用
- [x] 2.3 驗證點擊按鈕後正確重新載入票種資料
- [x] 2.4 驗證重新載入時顯示載入狀態提示
- [x] 2.5 驗證重新載入後保留當前選定的票種（如果仍存在）
- [x] 2.6 驗證重新載入失敗時顯示錯誤訊息
