## 1. 關鍵字必填驗證
- [x] 1.1 修改 `updateBookButtonState()` 函數：當 `ticketKeywordAutoSelect` 為 true 時，若 `ticketKeyword` 為空（trim 後無字元），則訂票按鈕應保持禁用狀態
- [x] 1.2 在 UI 上添加驗證提示：當勾選「啟用依關鍵詞自動選票」但關鍵字為空時，顯示提示訊息（例如：在輸入框下方顯示「請輸入關鍵詞」或類似提示）

## 2. 選票順序調整
- [x] 2.1 修改 `selectTicketByKeyword()` 函數：調整選票邏輯順序為：
  1. 先過濾票種列表，排除名稱中包含「老人」或「愛心」的票種
  2. 在過濾後的列表中，依序執行：關鍵字匹配 → 全票 → 單人套票 → 第一個
- [x] 2.2 確保當所有票種都被排除（全部為老人/愛心票）時，函數返回 null

## 3. 驗證
- [x] 3.1 驗證勾選「啟用依關鍵詞自動選票」但關鍵字為空時，訂票按鈕為禁用狀態
- [x] 3.2 驗證勾選「啟用依關鍵詞自動選票」且關鍵字有值時，訂票按鈕可啟用
- [x] 3.3 驗證選票邏輯會先排除老人/愛心票種，然後才進行關鍵字匹配等規則
- [x] 3.4 驗證當所有票種都是老人/愛心票時，自動選票會返回 null 並顯示適當錯誤
